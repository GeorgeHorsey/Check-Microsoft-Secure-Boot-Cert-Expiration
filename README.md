# Check-Microsoft-Secure-Boot-Cert-Expiration

# Step 1: 

Check if Secure Boot is Enabled (per computer) type this command into an elevated PowerShell. 
```
Confirm-SecureBootUEFI
```

If it is enabled this command will return "True". 
